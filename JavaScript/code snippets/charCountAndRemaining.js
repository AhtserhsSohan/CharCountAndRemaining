var yourPar = prompt('Write a paragrapg with 140 char.')
var charCount = yourPar.length;
var charRemaining =  140 - charCount  ;
alert(
    'Number of the characters written. ' + charCount + " Character remaining is " + charRemaining 
);
